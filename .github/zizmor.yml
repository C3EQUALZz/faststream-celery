# Configuration for zizmor (GitHub Actions static security analysis).
rules:
  dangerous-triggers:
    ignore:
      # Requires a writable token; the workflow checks out nothing.
      - labeler.yml
  unpinned-uses:
    config:
      policies:
        # pr_tests.yaml pins by version tag.
        "*": ref-pin
